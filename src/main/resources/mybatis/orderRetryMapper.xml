<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tony.user.mapper.OrderRetryMapper">

    <select id="getOrderRetry" resultType="java.util.Map">
        SELECT yuyue_id yuyueId FROM yuyue_pay_refund_retry WHERE state = 1 AND type = 1 and retry_num <![CDATA[<]]> 5
    </select>
</mapper>